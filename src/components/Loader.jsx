const Loader = () => {
  return (
    <div style={{
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      padding: "50px",
      fontSize: "1.2rem",
      color: "#1E40AF"
    }}>
      Loading...
    </div>
  );
};

export default Loader;
